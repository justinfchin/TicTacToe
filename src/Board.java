/**
 * Board Class for TicTacToe
 * @author Justin Chin
 * @author Cyndi Chin
 * @version 1.0
 * @since Jan. 3, 2017
 */
public class Board {
    private int size;           //# of rows and columns
    private int gridArray[][];  //**convert to images later?


    public Board(int size){
        this.size = size;                   //initialize size
        gridArray = new int[size][size];    //initialize data structure
    }

    /**
     * Checks if Board is Full
     * @return True if Board is Full, else False.
     */
    public boolean isBoardFull(){
        return
    }

}
